USE `quntm_equitymirror`;

-- This file is a lightweight seed example. Prefer running `php scripts/seed.php` which will generate
-- secure passwords and also produce a full `sql/final_dump.sql` for import via phpMyAdmin.

-- Example insert (created automatically by `scripts/seed.php`):
-- INSERT INTO users (username, password_hash, role, account_id, account_name, balance, status) VALUES
-- ('master', '<bcrypt_hash>', 'master', 'ALICEBLUE-1', 'Master Account', 125430.50, 'Connected');

-- Use `scripts/seed.php` to programmatically create users, followers, trades, risk settings and a final SQL dump.
